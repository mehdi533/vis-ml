# Example Configs (Canonical)

Use this folder for runnable templates and documented examples.

Conventions:
- File names end with `.example.yaml` where possible.
- Paths inside configs should be repo-root-relative.
- Keep examples lightweight and reproducible.
