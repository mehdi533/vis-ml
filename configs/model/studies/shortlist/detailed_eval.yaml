inherits: configs/model/studies/architecture/core.yaml

output_dir: results/thesis_model_results/outputs/shortlist_detailed_eval

training:
  epochs: 200
  save_test_predictions: true

sweep:
  models: [MLP, MTLSH, MTLGSH, PICNN_MTLSH]
