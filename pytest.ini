[pytest]
# Make the repo root importable so `research`, `models`, `scheduling`,
# `data_generation` resolve as packages during tests.
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
